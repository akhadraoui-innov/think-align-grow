## Changement

Dans `src/components/admin/AdminSidebar.tsx` ligne 357, remplacer `navigate("/")` par `navigate("/simulator")` pour que le bouton "Retour à l'app" amène l'utilisateur sur `/simulator` au lieu de la landing.

Aucun autre fichier impacté.