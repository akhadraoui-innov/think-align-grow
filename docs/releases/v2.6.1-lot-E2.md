# v2.6.1 — Lot E2 — Délivrabilité avancée

## Backend
- 3 queues pgmq priority lanes : `gi_email_transactional`, `gi_email_marketing`, `gi_email_bulk`.
- RPCs : `enqueue_email_priority`, `read_priority_email_batch`, `delete_priority_email`, `get_priority_lane_metrics`.
- Table `email_webhook_secrets` (RLS SaaS team) + RPCs `rotate_email_webhook_secret` / `get_email_webhook_secret`.
- Enrichissement `suppressed_emails` (provider_code, organization_id, source, bounced_at).
- Enrichissement `email_automation_runs` (delivered_at, bounced_at, complained_at, unsubscribed_at, provider_message_id).

## Edge functions
- **email-events** (publique, verify_jwt=false) : reçoit bounces/complaints/engagement Resend/SendGrid avec vérification HMAC stricte (Svix pour Resend, HMAC-SHA256 base64 pour SendGrid, hex generic).
- **process-email-priority-queue** (publique, déclenchée par cron) : draine les 3 lanes en ordre strict, batches 20/50/100, max 5 retries → DLQ.
- **trigger-email** : mapping `categoryForTemplate` → `priorityLane`.

## UI
- `EmailWebhookReceiverPanel` dans EmailProvidersTab : URL + secret + rotation + instructions par provider.
- `EmailPriorityLanesPanel` dans EmailReliabilityTab : 3 jauges live refresh 15s.

## Audit
- Linter Supabase : 0 finding.
- 3 queues `gi_email_*` créées et listées par pgmq.
- 6 RPCs E2 déployées.
- Table `email_webhook_secrets` créée avec RLS.
