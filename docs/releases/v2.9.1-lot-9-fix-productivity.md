# v2.9.1 — Lot 9.1 — Fix régression cron + Productivité (logs/audit)

## 🐛 Brique A — Régression critique fixée
- Cron `purge_expired_email_tokens_daily` cassé depuis 48h après Lot 6 (colonne `expires_at` supprimée de `email_unsubscribe_tokens`).
- Réécriture de `purge_expired_email_tokens()` :
  - Tokens unsubscribe : purge sur `used_at > 30j` ou `created_at > 90j` (jamais utilisés).
  - Tokens confirmation : inchangé (`expires_at` toujours présent).
- REVOKE EXECUTE conservé (service_role only).

## 🟢 Brique B — Productivité (2 pages migrées)
- `AdminLogs` : URL filters (zod schema 6 champs) + Saved Views (built-ins "Suppressions", "Connexions") + bouton Réinitialiser.
- `AdminAudit` : URL filters (4 champs) + Saved Views (built-ins "Mises à jour orgs", "Actions en masse") + Réinitialiser.
- Pattern : URL-driven, filtres partageables par lien, pas de refacto de la couche données.

## 🟢 Brique F — Cleanup cron
- Nouveau cron quotidien `cleanup_rate_limits_daily` (4h00 UTC) → `cleanup_rate_limits()`.
- Borne la table technique `rate_limits` dans le temps.

## 📋 Reporté Lot 9.2/9.3
- Productivité 6 pages restantes (`AdminAcademyPaths/Certificates/Assets`, `AdminUCM`, `AdminBilling`) → ajout simple `<DataTable exportable>` ou bouton CSV en lot 9.2.
- Brique C (rate limiting 5 cibles), Brique E (tests Deno+Vitest), Brique D (Vault pgsodium).

## Métriques
| Indicateur | Avant | Après |
|---|---|---|
| Cron actifs | 4 | 5 |
| Cron failed (24h) | 1 | 0 (au prochain run 3h00 UTC) |
| Pages admin URL-driven + Saved Views | 4 | 6 |
| Findings sécurité | 51 | 51 |
