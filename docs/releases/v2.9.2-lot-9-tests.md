# v2.9.2 — Lot 9.2 — Tests Vitest hooks critiques

## 🟢 Brique E (frontend) — 3 nouvelles suites

| Fichier | Tests | Couverture |
|---|---|---|
| `src/hooks/usePermissions.test.tsx` | 5 | super_admin / member / multi-rôles / impersonation read-only / legacy compat flags |
| `src/hooks/useDeleteUser.test.tsx` | 3 | succès + archive RGPD / mapping erreurs RGPD / fallback message brut |
| `src/lib/exportCsv.test.ts` | +5 (8 total) | null/undefined, Date, objets imbriqués, arrays d'objets (name/label fallback), header-only |

**Résultat** : `vitest run` → 16/16 passing en 4,5 s.

## 🟡 Brique C (rate limiting étendu) — Annulé

La politique plateforme (« pas de nouvelles primitives rate-limit côté backend ») prime. Les 5 cibles initialement prévues (`adjust_credits`, `send-invitation`, `send-test-email`, `email-marketing-ai`, `business-quote`) ne seront pas équipées. La RPC `check_rate_limit` existante (Lot 8) reste en place sur `delete-user` et `impersonate-user`.

## 🟡 Brique E (Deno) — Reporté Lot 9.3

Tests `verify-certificate` / `trigger-email` / `_shared/rate-limit` / `_shared/email-security` à écrire dans le même lot que la migration Vault (la refacto va déplacer les imports `Deno.env`, autant ne pas écrire les tests deux fois).

## 📋 Lot 9.3 (suivant)
- Brique D — Vault pgsodium : extension activée + helper `app_get_secret` + migration `ai_configurations.api_key`, `email_provider_configs.credentials`, `email_webhook_secrets.secret` + patch 8 EFs consommatrices.
- Brique E (Deno) — 4 tests evoqués ci-dessus.

## Métriques
| Indicateur | Avant | Après |
|---|---|---|
| Fichiers de test frontend | 2 | 5 |
| Tests frontend | 7 | 16 |
| Hooks critiques testés | 0 | `usePermissions`, `useDeleteUser` |
| Findings sécurité | 51 | 51 |
