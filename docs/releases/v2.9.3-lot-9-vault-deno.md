# v2.9.3 — Lot 9.3 : Vault pgsodium & tests Deno

**Date** : 2026-04-27
**Statut** : ✅ Déployé

## 🎯 Objectif

Mettre en place une couche de chiffrement Vault (pgsodium) pour les 3 tables sensibles, avec rétro-compatibilité totale et zéro régression.

## 🔐 Brique D — Vault pgsodium (3 tables)

### Infrastructure ajoutée
- Extension `supabase_vault` (pgsodium) activée
- Fonctions helper SECURITY DEFINER `service_role` only :
  - `app_get_secret(uuid, text)` — déchiffre + audit log dans `audit_logs`
  - `app_store_secret(text, text, uuid)` — créé/met à jour un secret Vault
  - `get_ai_api_key(uuid)` — RPC pour EFs IA
  - `get_email_provider_credentials(uuid)` — RPC pour EFs email
  - `get_email_webhook_secret(text, uuid)` — RPC pour bounces

### Colonnes ajoutées (rétro-compatibles)
- `ai_configurations.api_key_secret_id`
- `email_provider_configs.credentials_secret_id`
- `email_webhook_secrets.secret_secret_id`

### Triggers d'auto-chiffrement
- `BEFORE INSERT/UPDATE` sur les 3 tables : toute valeur en clair fournie est immédiatement chiffrée dans Vault et la colonne legacy est vidée.
- Audit confirmé avant migration : **0 secret en clair** à date → aucune migration de données existantes nécessaire.

## 🔧 Brique D bis — Patch des 8 EFs consommatrices

Pattern appliqué dans `resolveAIConfig()` de chacune :
- `ai-coach`, `ai-deliverables`, `ai-reflection`, `analyze-challenge`, `generate-toolkit`, `refine-toolkit`
- Si `api_key` vide et `api_key_secret_id` présent → RPC `get_ai_api_key`
- Fallback `LOVABLE_API_KEY` env si rien

Pattern appliqué pour les credentials email :
- `trigger-email`, `process-email-priority-queue`
- Si `credentials` JSONB vide et `credentials_secret_id` présent → RPC `get_email_provider_credentials`

Helper partagé créé : `supabase/functions/_shared/ai-config.ts`

## 🧪 Brique E — Suites Deno tests (3 fichiers)

| Fichier | Tests | Couverture |
|---|---|---|
| `_shared/email-security_test.ts` | 9 | sanitize HTML, XSS, javascript:, data:, tags interdits |
| `_shared/vault_helpers_test.ts` | 4 | RPCs Vault non callables par anon |
| `verify-certificate/index_test.ts` | 3 | CORS, validation input, certificat inconnu |

## 📊 Critères d'acceptation

| Critère | État |
|---|---|
| Extension Vault active | ✅ |
| 3 tables avec colonne `*_secret_id` | ✅ |
| 3 triggers auto-chiffrement | ✅ |
| 3 RPCs déchiffrement service-role only | ✅ |
| 8 EFs patchées avec fallback Vault | ✅ |
| 3 suites Deno tests créées | ✅ |
| Linter : 42 → 48 warnings (6 nouveaux SECURITY DEFINER, tous REVOKE'd → conformes baseline by-design Lot 7) | ✅ |
| Aucune régression UI/EF | ✅ |

## 🚫 Hors périmètre (validé en début de Lot 9)

- Brique C rate-limiting étendu (annulé — politique plateforme)
- Migration des 41 SECURITY DEFINER en INVOKER (Lot 10+)
- Tests Vitest auth/systemHealth/orgProvider (Lot 9.2 a couvert l'essentiel : permissions, deleteUser, exportCsv)

## 🔗 Fichiers modifiés

- `supabase/migrations/20260427_vault_pgsodium.sql` (nouveau)
- `supabase/functions/_shared/ai-config.ts` (nouveau)
- `supabase/functions/_shared/email-security_test.ts` (nouveau)
- `supabase/functions/_shared/vault_helpers_test.ts` (nouveau)
- `supabase/functions/verify-certificate/index_test.ts` (nouveau)
- `supabase/functions/{ai-coach,ai-deliverables,ai-reflection,analyze-challenge,generate-toolkit,refine-toolkit,trigger-email,process-email-priority-queue}/index.ts`
